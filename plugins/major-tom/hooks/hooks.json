{
  "description": "Injects the operator writing rule and session context before every reply; gates the points where work is authorized (workflow invocation, agent launch, skill invocation and the main session's own mutating calls) on a recorded intent for the current prompt, releasing the tool calls made inside an agent whose launch was already gated; on a fresh session start, reports when the installed plugin or this project's onboarded artifacts are out of date.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/version-check.js"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/writing-rule.js"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write|Bash|Agent|Workflow|Skill",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/think-gate.js"
          }
        ]
      }
    ]
  }
}
