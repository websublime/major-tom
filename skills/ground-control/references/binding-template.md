# Process binding - <project>

The project-specific half of ground-control: the skill holds the invariants, this file holds the slots. Generated by ground-control init on <date>; edit it freely, it belongs to the project. Imported from CLAUDE.md via @ so every session loads it: **pointers over prose**, every line costs context.

## Project

- North star: <default: correctness and completeness over speed>
- Owner (genuine forks and escalations go here): <name>
- Conversation language: <language> / Artifact language (code, docs, commits): <default: English>
- Capability rung: <1 scripted teams + worktree isolation | 2 subagents only | 3 solo with hats>

## Document roles

| Role | Bound to |
|---|---|
| Product truth | <docs/PRD.md, or "unbound: decision register carries product decisions"> |
| Decision register | <a section of product truth, or a standalone DECISIONS.md> |
| Interface SSOT | <docs/design-spine.md, an API spec, or "unbound: register carries interface decisions"> |
| Component plans | <docs/plans/<component>.md per component, or unbound> |
| Status registry | <docs/STATUS.md, the issue tracker, or "unbound: degraded mode"> |
| Roadmap | <docs/ROADMAP.md, or unbound> |
| Knowledge base | <.knowledge/, or "unbound: session memory only"> |

Downstream docs for the decision-change checklist (everything that restates decisions, names, or versions): <README.md, CI docs, install docs, ...>

## Team roster

Every role slot defaults to the plugin agent of the same name (coordinator, architect, researcher, project-manager, product-validator, code-reviewer, qa, refactor-specialist, vcs-operator); list only what this project overrides, plus the fetched specialists.

| Slot | Resolved to |
|---|---|
| domain specialist | <fetched by init; per component when a monorepo: component -> agent> |
| <any overridden slot> | <agent, or a harness built-in> |

Agents directory (where init fetches specialists): <default: https://github.com/ayush-that/sub-agents.directory>

An unbound slot falls back to the strongest generalist agent available; the report must say when that happened.

## Trackers

Two roles, usually one system: **work intake** (where tickets arrive) and **status registry** (where execution is tracked). Adapter access resolves in a fixed order: MCP when connected, else CLI, else REST, else the status file.

- Work intake: <tracker from the table>
- Status registry: <tracker from the table>
- Ticket workspaces: <docs/tickets/>

| Tracker | Kind | Access |
|---|---|---|
| <name> | <status-file | beads | unblock | github | jira | linear> | <MCP server name; CLI command; REST base URL + credential env var names (e.g. JIRA_BASE_URL, JIRA_EMAIL, JIRA_API_TOKEN), names only, never values; or the file path docs/STATUS.md> |

| Verb | This repo's action |
|---|---|
| fetch <id> | <e.g. unblock MCP issue get; `bd show <id> --json` plus comments; Jira REST issue + comments + links; read the STATUS.md row and every doc it links> |
| ready | <e.g. next unblocked row in STATUS.md; `bd ready`; a board query> |
| claim | <e.g. flip the row to in progress; `bd update <id> --claim`> |
| update | <e.g. edit the row in the same commit as the work> |
| close-on-merge | <e.g. flip the row to done on PR merge; `bd close <id>`> |

## Knowledge base

- Path: <.knowledge/>
- Layout: `memory/` holds one fact per file plus an `INDEX.md`; CLAUDE.md imports the index (`@.knowledge/memory/INDEX.md`) so it loads every session.
- Rule: durable project learnings (context behind decisions, gotchas, environment quirks) land here in the same session they are learned. A fact an authoritative doc should own graduates into that doc and leaves a pointer behind.

## Conventions

- Branch naming: <e.g. t<id>-<slug>, off <main>>
- Commits: <default: Conventional Commits, atomic>
- PRs: one per task, opened by the agent with gate verdicts; merged by <owner or approver>. Local-only mode (no remote): <who signs off>
- Security lens on Verify when: <the project's triggers: input handling, storage, sync, permissions, ...>

## Hard-rule additions

The skill's shipped hard rules always apply and cannot be removed here. Project rules add below:

- <e.g. no new dependencies without a decision entry>
