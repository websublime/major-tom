# The compliance budget

At the bottom executor tier, attention is conserved: forced prose artifacts reallocate it between disciplines, they do not add capacity (eval rounds 12-15, s9 family). What holds anyway: mechanical guards (hooks fire outside the attention budget) and structure (teams where each agent holds few constraints). Replicated skill gains at that tier: doc-first drift repair and truthful TRACK disclosure.
