# eval archive

This directory holds the eval material for the retired **ground-control** layer, kept as published history (the project's north star is failures published, so the record is preserved, not deleted).

- `scenarios/` - the drift-trap and delegation-trap fixtures: s9-drift-trap, s9b-drift-trap-sparse, s10-delegation-trap.
- `cases/` - the s9-drift-trap case study.
- `results/` - rounds 11 to 16 (the drift-trap family and the compliance-budget arc).
- `workflow-s10.js` - the s10 delegation-trap harness.

These measured ground-control, a project-scale skill retired by the kill-ground-control change. The active eval (think, act, prove) lives in the parent `eval/` directory. The numbers here stand as recorded; treat them as history of a retired layer.
